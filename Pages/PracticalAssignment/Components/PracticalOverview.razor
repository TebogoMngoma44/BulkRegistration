@* Components/PracticalAssignment/PracticalOverview.razor *@
@using MudBlazor
@attribute [Authorize]

@* 
<CascadingAuthenticationState>
    <AuthorizeView Context="UserList">
        <Authorized>
*@
<MudPaper Class="overview-section" Elevation="0">
    <MudText Class="instruction-text">
        This practical assignment requires you to demonstrate advanced Excel skills by creating comprehensive spreadsheets that analyze real business data. You will work with sales, financial, and operational datasets to create formulas, charts, pivot tables, and generate actionable business insights.
    </MudText>
</MudPaper>
@*                   </Authorized>
        <NotAuthorized>
            @{
                NavigationManager.NavigateTo("/auth/logout");
            }
        </NotAuthorized>
    </AuthorizeView>
</CascadingAuthenticationState> *@

@code {
    // No additional data needed - just the simple overview text
}

<style>
    .overview-section {
        background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        border-radius: 16px;
        padding: 32px;
        border: 1px solid rgba(30, 58, 95, 0.1);
    }

    .instruction-text {
        font-size: 16px;
        line-height: 1.8;
        color: #444;
        margin-bottom: 24px;
    }

    .highlight-box {
        background: #e8f0ff;
        border-left: 4px solid #1e3a5f;
        padding: 24px;
        border-radius: 12px;
        margin-top: 24px;
    }

    .objective-item {
        display: flex;
        align-items: flex-start;
        gap: 12px;
        margin-bottom: 16px;
        padding: 16px;
        background: white;
        border-radius: 12px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }

        .objective-item:last-child {
            margin-bottom: 0;
        }
</style>